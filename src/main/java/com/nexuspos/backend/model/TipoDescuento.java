package com.nexuspos.backend.model;

/**
 * ENUM - Tipo de Descuento
 */
public enum TipoDescuento {
    PORCENTAJE,    // Descuento por porcentaje (ej: 10%)
    MONTO_FIJO     // Descuento por monto fijo (ej: 5000 Gs)
}
